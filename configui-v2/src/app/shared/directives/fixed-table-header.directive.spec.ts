/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FixedTableHeaderDirective } from './fixed-table-header.directive';

describe('Directive: FixedTableHeader', () => {
  it('should create an instance', () => {
    let directive = new FixedTableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
