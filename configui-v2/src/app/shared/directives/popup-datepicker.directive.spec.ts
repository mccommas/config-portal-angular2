/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PopupDatepickerDirective } from './popup-datepicker.directive';

describe('Directive: PopupDatepicker', () => {
  it('should create an instance', () => {
    let directive = new PopupDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
